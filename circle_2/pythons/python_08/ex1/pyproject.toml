[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
description = "Matrix data analysis"
authors = ["student"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "*"
numpy = "*"
matplotlib = "*"
requests = "*"
